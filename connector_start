curl -X POST -H "Content-Type: application/json" --data '{
  "name": "neo4j-sink-orderpaid",
  "config": {
    "connector.class": "org.neo4j.connectors.kafka.sink.Neo4jConnector",
    "tasks.max": "1",
    "topics": "order-paid",
    "neo4j.uri": "bolt://neo4j:7687",
    "neo4j.authentication.type": "BASIC",
    "neo4j.authentication.basic.username": "neo4j",
    "neo4j.authentication.basic.password": "password",
    "neo4j.database": "neo4j",
    "key.converter": "org.apache.kafka.connect.storage.StringConverter",
    "value.converter": "org.apache.kafka.connect.json.JsonConverter",
    "value.converter.schemas.enable": "false",
    "errors.tolerance": "all",
    "neo4j.cypher.topic.order-paid": "MERGE (u:User {ext_id: event.user_id}) MERGE (pr:Product {ext_id: event.product_id}) MERGE (u)-[r:PURCHASED]->(pr) SET r.date = datetime({epochMillis: event.timestamp}), r.type = 3, r.name = 'PURCHASED', r.rating = event.rating"
  }
}' http://localhost:8083/connectors